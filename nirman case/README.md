# Nirmaan — Intro Scoring Demo

## Quick run
1. Put the uploaded files into the project folder (Case study for interns.xlsx, Sample text for case study.txt).
2. (Optional) Create venv and install:
   python -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
3. Run:
   python app.py
4. Open http://localhost:5000

## CLI demo
python scorer.py
or
python run_example.py
